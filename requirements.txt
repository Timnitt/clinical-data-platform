# Core data work
pandas==3.0.5

# Plotting (used for age-distribution charts in the notebooks)
matplotlib==3.11.1

# Jupyter kernel so the notebooks run in VS Code / JupyterLab
ipykernel==7.3.0

# Testing
pytest==9.1.1
